# Content-based image classification module for Clean-Backup.
# Provides a multi-stage pipeline (EXIF → heuristic → ML scene → face detect
# → face recognition → tag resolution) to categorize media files.
